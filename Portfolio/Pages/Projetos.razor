@page "/projetos"

<h1 class="text-2xl font-bold mb-4">Projetos</h1>
<div class="grid grid-cols-1 md:grid-cols-2 gap-6">
    <div class="p-4 shadow-lg rounded-lg border">
        <h2 class="font-bold">DynamicDbContext</h2>
        <p>Contexto dinâmico para múltiplos bancos de dados, inicialmente PostgreSQL.</p>
        <span class="text-sm text-gray-600">.NET, EF Core, PostgreSQL</span>
    </div>
    <div class="p-4 shadow-lg rounded-lg border">
        <h2 class="font-bold">API com JWT Refresh</h2>
        <p>API segura em .NET com autenticação JWT e renovação automática de tokens.</p>
        <span class="text-sm text-gray-600">.NET, JWT, Blazor</span>
    </div>
    <div class="p-4 shadow-lg rounded-lg border">
        <h2 class="font-bold">Worker Service com Selenium</h2>
        <p>Automação de processos com Selenium e C# executando em background service.</p>
        <span class="text-sm text-gray-600">.NET, Selenium, RPA</span>
    </div>
</div>

@code {

}
